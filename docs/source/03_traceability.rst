.. trace tables (hazard→req→test→evidence etc.)

Traceability
============

Hazard → Requirement → Verification → Evidence
----------------------------------------------

.. needtable::
   :types: hazard,fsr,verif,evidence
   :columns: id;title;phase;sil_level;mitigates;verifies;produces